
/*----------------------------------------------------------------------------**
**              Copyright @ 1994 HUJI. All rights reserved.                   **
**----------------------------------------------------------------------------*/

/*----------------------------------------------------------------------------
				pnt_basic.h
 Purpose : access macros to abstract points.
------------------------------------------------------------------------------*/

#ifndef _pnt_basic_h
#define _pnt_basic_h

/*----------------------------------------------------------------------------*/
/* Generic Pnt Access Macros */

#define PntX(p)      ((p)->el[0])
#define PntY(p)      ((p)->el[1])
#define PntZ(p)      ((p)->el[2])
#define PntW(p)      ((p)->el[3])
#define PntI(p,i)    ((p)->el[i])

#define Pox(p)     ((p)->el[0])
#define Poy(p)     ((p)->el[1])
#define Poz(p)     ((p)->el[2])
#define Pow(p)     ((p)->el[3])
#define Poi(p,i)   ((p)->el[i])

#define PntEl(p)   ((p)->el)

#endif

/*----------------------------------------------------------------------------
				E O F
------------------------------------------------------------------------------*/

